dist
coverage
node_modules
package-lock.json

# Legacy files stay unchanged until their owning module is touched structurally.
README.md
index.html
src/actions.ts
src/data.ts
src/env.ts
src/metrics.ts
src/redesign.css
src/styles.css
src/supabase.ts
src/ui-icons.tsx
src/visual-hardening.css
src/visual-polish.css
src/vite-env.d.ts
tsconfig.json
vite.config.ts

# Выгрузка каталога из базы: файл производный, его формат задаёт скрипт
# scripts/dump-knowledge-catalog.mjs. Переформатировать его значит спорить с генератором.
knowledge-catalog.json

# Индекс статей для приложения: файл производный, его формат задаёт сборка
# (buildArticlesIndex). Причина та же — переформатирование спорит с генератором, и
# следующая сборка вернёт всё назад.
knowledge-articles.json

# Разобранная история репозитория: файл производный, его формат задаёт
# scripts/build-changelog.mjs. В git его нет вовсе.
src/generated/
