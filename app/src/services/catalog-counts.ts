/**
 * Сколько чего в каталоге — одним числом на весь продукт.
 *
 * До 31.08.2026 эти числа были написаны руками в четырёх местах: `llms.txt`,
 * статический слепок в `index.html`, описания разделов в `seo.ts` и текст предрендера.
 * Карточка доски `chisla-v-llms-txt` описывала это как «статическая копия каталога,
 * который правится миграциями — тот самый механизм, который для index.html был
 * отвергнут».
 *
 * Копия уже разошлась. Слияние триггеров (`20260821120000`, `20260827210000`) свело 28
 * пусковых моментов к 25, а число 28 осталось во всех четырёх местах и уехало на прод.
 *
 * Теперь источник один: `content/knowledge/claims.lock.json` — машинный файл,
 * пересобираемый из базы командой `node scripts/knowledge-lock.mjs`. Он же держит хэши
 * утверждений, то есть пересобирается при любой правке каталога, а не отдельным
 * усилием памяти.
 *
 * В бандл уезжают только числа: остальное дерево замка сюда не импортируется.
 */
import lock from '../../../content/knowledge/claims.lock.json';

export const CATALOG_COUNTS: {
  facts: number;
  myths: number;
  triggers: number;
  replacements: number;
} = lock.counts;
