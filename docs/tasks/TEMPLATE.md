# T-ГГГГММДД-NN · [тема] Короткое название

    Тип: задача
    Тема: <одна из списка в docs/tasks/README.md>
    Карточка доски: <id из docs/board/cards.json>
    Ветка: task/<id>
    Из разбора: R-…md (или «нет»)
    Поставлено: <дата> · чат: <url>

## Предпосылки — проверить первыми командами

Каждая строка: команда и что должно быть в ответе. **Не сошлось — остановиться и сказать,
а не чинить по дороге.** Постановка написана до проверки и может быть неверна.

- `bash scripts/state.sh` — ветка чистая, доска без ошибок, ноль неотправленного
- <предпосылка задачи: файл существует / прод отвечает / миграций столько-то>

## Доступы

По умолчанию — обычные доступы исполнителя на машине владельца: `push`, `ssh VPS`,
браузер. Владельца про них не спрашивают, они не меняются от задачи к задаче.
Заполняется только строка **сверх обычного** — и то, чего у постановщика нет
(`docs/tasks/README.md`, раздел «Доступы контура»). Фактическое состояние показывает
`bash scripts/state.sh`.

    Сверх обычного: —

## Инварианты — нарушать нельзя

3–7 строк, **каждая со ссылкой на источник**: принцип (`P17`), ADR, раздел `AGENTS.md`
или документ. Инварианты предлагает постановщик, а не вспоминает владелец; строка без
источника помечается «предложено — подтвердить».

## Что сделать

Списком. Без «заодно».

## Приёмка — команды и ожидаемый вывод

Проверяемое, а не «работает». Показывать вывод, а не пересказ.

## Не делать

То, что напрашивается и в эту задачу не входит. Найденное попутно — `Board: new` в том же
коммите, не в эту ветку.

## Результат — заполняет исполнитель

- коммит(ы), PR;
- вывод проверок приёмки;
- что осталось незакрытым и почему;
- трейлер `Board: done <id>` в последнем коммите.
