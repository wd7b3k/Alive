# T-20260905-11 · Выложить на прод всё, что сделано и лежит невыложенным

**Тема:** release · **Ветка:** `task/T-20260905-11` · **Статус:** todo

Опись (Р0) уже проведена — ниже фактическое состояние на 05.09.2026, снятое прямым
обходом git после `git fetch --prune --all`. Исполнителю остаются решения владельца,
слияния и выкладка. Числа перепроверить одной командой перед началом: за сутки они
меняются.

`origin/main` = `1f67d3e` (Merge pull request #82). Локальный `main` — 0 впереди,
42 позади, чистый. Миграций в `main` — 64, последняя `20260905130000_v3_funnel_stages`.

---

## Что опись изменила в исходной постановке

1. **`chore/vps-deploy` и `chore/process-rules` уже полностью в `main`** (0 коммитов
   впереди). Шага по сведению прод-ветки нет — вместо него проверка, что прод собирается
   с `main`.
2. **Номера задач на 05.09 заняты до 10-го** одиннадцатью **незакоммиченными** файлами в
   `docs/tasks/` (i18n и knowledge). Отсюда номер этой задачи — 11.
3. **`T-20260905-01` означает две разные вещи**: ветка `task/T-20260905-01` — раздел
   «Что сделано», а файл `docs/tasks/T-20260905-01-i18n-foundation.md` — основа i18n.
   Разойдутся при первом же слиянии. Развести до слияния.
4. **`ADR-0017` занят дважды**: в `main` это `ADR-0017-knowledge-card-addresses.md`, в
   `feat/pilot-feedback` — `ADR-0017-participant-feedback.md`. Перенумеровать второй в
   `0019` (0018 занимает `ADR-0018-changelog-from-git` из ветки `task/T-20260905-01`).
5. **Миграции задним числом.** Ветки `task/counter-health`, `agent/monitoring-truth`,
   `agent/source-funnel-rebased` несут миграции с датами `2026083118…` и `2026083119…`,
   а на проде уже применены миграции от 05.09. Появление более старой миграции после
   более новых — отдельный риск: проверить по коду применятеля, что он их не пропустит
   и не применит вне порядка. Пока это не проверено, ни одна из трёх веток не сливается.

---

## Незакоммиченное в рабочей копии (14 позиций, ветка `main`)

`HANDOFF.md`, `content/`, `docs/RUNBOOK_KNOWLEDGE_WEEKLY.md`, одиннадцать файлов
`docs/tasks/` (`R-20260905-01-i18n-english`, `T-20260905-01…07` по i18n,
`T-20260905-08…10` по базе знаний).

Это постановки, а не код. Закоммитить одной веткой в корзине Б, ничего не редактируя по
существу. `content/` — проверить, что это: если выгрузка каталога, в git ей не место
(см. ветку `fix/deploy-drop-catalog-dump`).

---

## Опись веток: что не входит в `origin/main`

**Корзина А — слить и выложить**

| Ветка | +коммитов | Что трогает |
|---|---|---|
| `task/T-20260905-01` | 5 | раздел «Что сделано», `app/src`, три скрипта, ADR-0018 |
| `chore/nothing-unverified-rule` | 1 | `AGENTS.md`, тест, `supabase/tests` |
| `chore/ops-fixes` | 3 | `infra/server`, `infra/monitoring`, `docs/ROLLOUT.md` |
| `feat/server-apply-migrations` | 1 | `infra/server`, `docs/ROLLOUT.md` |

Дубль: `chore/nothing-unverified` — та же работа другой веткой, удалить, не сливая.

**Корзина Б — слить, выкладкой не считать**

| Ветка / позиция | Что |
|---|---|
| незакоммиченные постановки (14 позиций выше) | документы |
| `docs/ai-session-2026-08-20-strategy-audit` | +1, запись сессии |
| `docs/counter-diagnosis` | +2, записи сессии — **воркtree занят другой сессией**, не трогать без её завершения |

**Корзина В — решение владельца, не сливать самостоятельно**

| Ветка | +коммитов | Почему |
|---|---|---|
| `task/knowledge-base` | 8 | `content/knowledge` — живой медицинский контент, стоп-лист |
| `feat/pilot-feedback` | 11 | канал обратной связи; ADR-0017 конфликтует; миграций нет при изменённом `DATA_MODEL.md` — похоже, работа не закончена |
| `agent/monitoring-truth` (6) / `origin/agent/monitoring-truth` (3) / `origin/agent/monitoring-truth-clean` | | три версии одной работы, выбрать одну; миграция задним числом |
| `task/counter-health` | 2 | та же миграция `20260831180000` (содержимое совпадает, `5d571096b42b`) |
| `agent/source-funnel-rebased` | 1 | миграция `20260831190000` задним числом |
| `merge/pilot-with-vps` | 1 | от 27.08, `app/public` и бренд — вероятно, перекрыто; проверить, не воскрешает ли старое |
| августовские: `origin/v3.0-platform` (30), `v3.0-redesign` (8), `v3.0-hardening` (14), `agent/v4.0.0-alpha.1` (222), `…hotfix.1` (225), `agent/owner-vision-delivery-protocol` (225), `agent/r1-data-evidence-admin` (100), `agent/v3.1-behavioral-depth-together` (67), `agent/product-strategy-foundation` (22), `migration/standalone-repo` (1) | | история до 20.08, не сливать. Решение: пометить архивными и удалить с origin или оставить как есть — но перестать считать невыложенной работой |

**Стоп-лист** (в корзину В автоматически): миграции каталогов фактов и мифов,
поднятие `CONSENT_VERSION`, медицинские формулировки, монетизация, RLS, ключи.

---

## Права и самостоятельность

Всё, что можно из сессии, делается из сессии: пуш, `gh pr create`, `gh pr merge`,
удаление слитых веток, коммиты, правка документов. `gh` авторизован 05.09.
Владельца дёргать только там, где физически нельзя: доступ к серверу и решения по
корзине В. Просьба — с готовой командой, одной строкой, абсолютные пути, PowerShell 5.1,
без `&&`.

---

## Р1 · Развести номера и ADR

До первого слияния, одним коммитом в этой ветке:

- переименовать файл задачи в ветке `task/T-20260905-01` так, чтобы номер не совпадал с
  i18n-постановкой (номер выбрать свободный по факту, ветку не переименовывать — она уже
  на origin и упомянута в записях сессии; расхождение имени ветки и файла записать
  строкой в самом файле);
- в `feat/pilot-feedback` перенумеровать ADR-0017 в 0019, поправить ссылки на него;
- проверить, что ни один свободный номер ADR не занят второй раз в других ветках
  корзины В.

## Р2 · Слияние

Корзина Б целиком → корзина А, от инфраструктуры к продукту, внутри — от старого к
новому. По одной ветке; на каждой до PR зелёные: `tsc --noEmit`, `eslint`,
`prettier --check`, `vitest run`, `npm run build`, `scan-bundle-for-secrets.mjs`,
`board.mjs check`. PR создаётся и мержится из сессии, ветка удаляется, карточка
закрывается трейлером `Board: done <id>`. Способ слияния — тот же, что у PR #82, чтобы
разбор коммитов в `/admin/releases` не пришлось менять.

Ветка, которая не сливается механически, немедленно переезжает в корзину В. Героического
разрешения конфликтов в чужой незавершённой работе в этой задаче нет.

## Р3 · Версия и публичная запись

Номер не поднимался с 27.08, на проде накопилось больше двухсот коммитов, о которых
публичный `/releases` молчит.

- поднять `version` в `package.json`;
- одна запись в `app/src/redesign/releases.ts` — **заголовок и один абзац**, формат от
  27.08, поля `changes` нет; писать для человека, который бросает курить, тон по
  `TONE_OF_VOICE.md`;
- `releases.test.ts` проходит без правок теста.

## Р4 · Выкладка

1. Дамп базы до миграций — `pg_dump` только через `docker exec` (на хосте `psql` 16,
   сервер 17). Убедиться, что `habitoff-verify` проходил.
2. Число миграций до и после назвать; сверить с числом файлов в `supabase/migrations/`
   (сейчас в `main` — 64).
3. `git pull` в `/srv/alive/repo`, затем `/srv/alive/deploy.sh`.
4. Проверка: `version.json` с уникальным параметром строки запроса и `cache: 'no-store'`
   отдаёт новый отпечаток; `readlink -f /srv/alive/current` — новый каталог;
   `scripts/check-deploy-drift.mjs` чист.
5. Живьём: вход через Google, вход через Яндекс, разбор тяги, `/knowledge`, `/admin` и
   его разделы (включая новый «Что сделано»), публичный `/releases` с новой записью,
   пять публичных адресов отдают свои заголовки и canonical.
6. Мониторинг: восемь проверок зелёные, алерт не пришёл.

**Откат** — переключение симлинка `/srv/alive/current` на предыдущий каталог. Для
миграций отката нет, поэтому п. 1 не пропускается. Порядок отката написать до выкладки.

Если доступа к серверу из сессии нет — команды владельцу выдаются готовыми, по одной,
с абсолютными путями; непроверяемые из этой среды помечаются непроверенными.

---

## Приёмка

- `check-local-drift.mjs` не находит мест, существующих только на машине, кроме
  перечисленных в корзине В;
- `check-deploy-drift.mjs` чист: прод = `origin/main`;
- прод отвечает новым отпечатком, шесть пунктов живой проверки пройдены;
- ни одного файла ADR с повторяющимся номером; ни одного файла задачи с номером,
  означающим две разные работы;
- `docs/CURRENT_STATE.md`, `docs/NAVIGATOR.md`, доска обновлены той же задачей;
- парная запись сессии: что выложено, что осталось в корзине В и почему, все расхождения
  между этим текстом и фактическим состоянием репозитория и сервера.
