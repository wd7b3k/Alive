# Текущее состояние Habitoff

## Ребрендинг 26.08.2026

До 26.08.2026 продукт назывался **ALIVE**. Решение и его границы —
[`decisions/ADR-0003-rebrand.md`](decisions/ADR-0003-rebrand.md).

- Имя: **Habitoff**. В прозе — одно слово, одна заглавная; `HabitOff` и `habitoff`
  допустимы только как технические идентификаторы.
- Домен `habitoff.ru` оплачен и с 27.08.2026 является рабочим адресом продукта.
- **Слаг репозитория остаётся `wd7b3k/Alive`.** Переименование репозитория ломает
  remotes у всех клонов и является отдельным инфраструктурным действием. Каталоги на
  сервере по той же причине остались `/srv/alive`.
- Знак: разомкнутое кольцо с точкой в разрыве, `app/src/assets/brand-mark.svg`.
  Om выведен по прямому запросу владельца — исключение, предусмотренное
  `RELEASE_POLICY.md` §11. Om-гейт в `docs/V3_VISUAL_UX_BASELINE.md` снят,
  Om-чекбокс в `releases/v3.0-platform/VALIDATION.md` закрыт как снятый, а не пройденный.
- Растровый комплект иконок и OG-картинка перевыпущены. PNG-лого на 363 КБ удалён:
  словесный знак теперь SVG на 2,8 КБ и инлайнится в бандл.

**Палитра не менялась, и это решение, а не пропуск.** Предложение вывести `--r-lime`
отозвано: лайм — основной цвет действия, им залиты CTA «Меня тянет» и кнопка «Войти»,
и он же кодирует уровень «хорошо подтверждено» и тип карточки-мифа. Его вывод
обесцветил бы главное действие и переписал эпистемическую разметку побочным эффектом
переименования. Знак использует существующие `--r-teal` и `--r-amber`; новых токенов
не заведено. Вопрос о втором акценте остаётся открытым и требует своего решения.

**Анимационный промт от 23.08 не исполнен — сознательно.**
`css-invariants.test.ts` требует ровно одного объявления `.r-button`, `.r-button.primary`
и `.r-button.ghost`; главная CTA «Меня тянет» — не `.r-button.primary`, а составная
карточка в двух местах `RedesignApp.tsx`, и её анимация потребовала бы правки разметки.
count-up, draw-in и skeleton требуют JavaScript на самом тяжёлом экране при бандле около
590 КБ и 30 запросах на первом экране.

Условием возврата было названо «после того как переезд закроет вес бандла и число
запросов». **Переезд этого не сделал и не мог: он решал доступность из РФ, а не вес
клиента.** На 27.08.2026 стартовый JS — 587 КБ одним чанком (165 КБ в gzip), число
запросов не менялось. Условие остаётся невыполненным, и это отдельная задача, у которой
пока нет владельца в плане.

**Заодно закрыто ребрендингом:** `app/package.json` — `alive-web` → `habitoff-web`,
версия `3.0.0-dev` → `3.1.0` (не совпадала с выпущенным v3.1); устаревшая строка
«Google нужен только для входа» переписана провайдер-независимо.

**`grep -ri alive` по репозиторию не пуст, и это объяснено.** Не тронуты:
`supabase/migrations/**` (там `alive` — имена функций, таблиц, колонок и значения ключей,
на которые ссылаются строки в проде), имена RPC в коде приложения, адрес
`alive.hmnos.ru`, `releases/**`, `docs/ai_sessions/**`, `ADR-0001` и `ADR-0002` — записи
о том, что было сделано под прежним именем. К ним добавились каталоги на сервере
(`/srv/alive`) и системный пользователь `alive`.

## Статус

Habitoff — самостоятельный private repository `wd7b3k/Alive`.

**27.08.2026 продукт переехал на собственную инфраструктуру.** Cloudflare Pages и
Supabase Cloud из контура убраны полностью, включая DNS. Продукт живёт на одном сервере в
Москве по адресу `https://habitoff.ru`.

Гипотеза, ради которой затевался переезд, подтверждена: **с российских мобильных сетей
сайт открывается мгновенно.** Проверено ещё на голой статике, до переноса бэкенда.

Предыдущая веха — v3.1, выпущенная 24.08.2026 на `alive-aw2.pages.dev`; release unit
`releases/v3.1-together-facts-meanings/`. Что вышло к людям: «Факты», «Смыслы» на
`goals_catalog`, «Вместе», закрытый экран здоровья продукта, выгрузка и удаление аккаунта.

`wd7b3k/Alive` — единственный source of truth. Код, миграции, продуктовые правила и
релизные гейты меняются сначала в репозитории; чат и SSH-сессия на сервере их не
переопределяют.

## Живая инфраструктура

- Frontend: React + TypeScript + Vite, статика из `/srv/alive/current`.
- Хостинг: собственный VPS Timeweb Cloud, Москва, Ubuntu 24.04 LTS, 4 vCPU / 8 ГБ / 80 ГБ.
- Веб-сервер: Caddy, сертификаты Let's Encrypt, `www` редиректит на apex.
- Домен: `habitoff.ru`, регистратор и NS — Рег.ру. Cloudflare не участвует нигде.
- Бэкенд: self-hosted Supabase — PostgreSQL 17.6 + RLS, GoTrue 2.189, PostgREST 14.12,
  edge-runtime 1.74, Envoy как шлюз. Выключены `realtime`, `storage`, `imgproxy`,
  `supavisor`.
- Наружу открыты только 22, 80, 443; всё остальное слушает `127.0.0.1`.
- Вход: Google встроенным провайдером, Яндекс — собственным мостом в edge-функциях.
- Выкладка: `/srv/alive/deploy.sh`, два гейта, атомарное переключение симлинка,
  пять последних сборок на диске.
- GitHub CI: Node `22.12.0`, `npm ci`, format check, lint, typecheck, тесты, production
  build, скан бандла на секреты и прогон RLS-изоляции на чистом PostgreSQL.

Подробности — [`INFRASTRUCTURE_STATE.md`](INFRASTRUCTURE_STATE.md), причины ухода —
[`decisions/ADR-0004-self-hosted-ru-contour.md`](decisions/ADR-0004-self-hosted-ru-contour.md).

`alive.hmnos.ru` и облачный проект остаются включёнными: старый прод не выключается две
недели после перевода трафика, и на нём всё ещё живут пользователи, которых предстоит
перенести.

## Ключевое решение после первого platform bootstrap

Первый web-shell был технически рабочим, но продуктово значительно беднее legacy v2.7.
Это признано regression.

Создан обязательный baseline `docs/V3_PARITY_BASELINE.md`:

**v3.0 не может считаться готовым, если новый пользователь получает менее глубокий
продукт, чем v2.7.**

Новая архитектура должна сохранить минимум глубины v2.7 и добавить утверждённые
возможности v3.

## Реализовано

### Product UI

- universal onboarding / personal baseline;
- cigarette / hookah / vape как отдельные raw products;
- product role: `target_dependency` / `cessation_bridge`;
- Сегодня — action screen, current metrics, attention links, recent episodes;
- guided craving flow: product → trigger → need → 3 contextual replacements → outcome;
- quick nicotine fact logging без выдумывания craving score;
- evening check-in;
- Связки — automatic trigger map + private user Links;
- Смыслы — global universal catalog + private CRUD/UGC workflow;
- Путь — 7-day динамика, raw products, personal replacement effectiveness, Freedom Fund,
  rewards;
- Profile/baseline;
- Эксперимент — methodology, assumptions, limitations, privacy;
- Релизы;
- удаление ошибочного/тестового эпизода с пересчётом из оставшихся фактов;
- inline explanations / `nothing unexplained` pattern;
- публичный просмотр опубликованного каталога до входа (`PublicHome` в `RedesignApp.tsx`);
- «Факты и Мифы» — карточки с уровнем доказательности, границами применимости и
  проверенными источниками; отдельный раздел плюс контекстный показ в потоке тяги, в
  Связках, на Сегодня и на первом экране до входа.

### Content depth

Каталоги приезжают миграциями и на новом сервере развёрнуты целиком:

- 28 published triggers;
- 74 published replacements;
- 106 trigger→replacement relations;
- 13 universal Meanings;
- 18 целей;
- 19 фактов и 19 мифов с проверенными источниками;
- 5 universal identity scripts;
- 13 support messages;
- 7 rewards;
- 18 карточек `awareness_content` — лежат в базе, ни один экран их пока не показывает.

Правило раздела «Факты» вынесено в схему, а не в договорённость: `scope_note_ru` —
`not null`, поэтому ни одна карточка не выходит без указания границ доказательства, а
опубликованная карточка уровня A или B без источника отклоняется триггером. Три
популярных утверждения сознательно не публикуются, потому что источника у них нет:
«тяга длится 3–5 минут», «вейп на 95 % безопаснее» и любое соотношение
«один кальян = N сигарет».

### Урок, который стоит помнить

В августе 2026 обнаружилось, что боевая база разошлась с репозиторием полностью: часть
замен существовала только в проде, часть строк там переименовали, метаданные
доказательности были в проде и отсутствовали в репозитории, и двадцать колонок были
добавлены прямо в прод. Ничего из этого не было оформлено миграцией, и правило
«репозиторий — единственный источник истины» тихо перестало действовать.

Расхождение сведено миграциями `20260821130000` (колонки) и `20260821140000` (значения);
проверено пересборкой базы из миграций и сверкой агрегатных SHA-256 поле в поле.

Все четыре расхождения за историю проекта родились из ручной правки через SQL Editor.
Своя база убрала эту кнопку — но правило теперь ещё и проверяется машиной при каждой
выкладке: число файлов миграций обязано равняться числу записей в
`supabase_migrations.schema_migrations`.

### Данные и privacy

Полный список миграций — в `supabase/migrations/`. На 27.08.2026 их 45, и все 45
применены к базе; расхождения нет.

RLS защищает все приватные сущности: 50 таблиц `public`, ни одной без включённой
политики. Права разведены явно — `anon` читает 42 таблицы (опубликованные каталоги),
`authenticated` имеет права на 45. Приватные таблицы для `anon` отбиваются на уровне
грантов, то есть раньше политик.

Анонимный доступ, добавленный 22.08 ради того, чтобы продукт открывался до входа,
намеренно узкий: `select` только, `published = true` только, только на редакторские
каталоги. `supabase/tests/local/03_rls_isolation_test.sql` утверждает именно это для
роли `anon` — будущая миграция, расширяющая анонимную поверхность, провалит тест, а не
протечёт молча.

Публичный `SECURITY DEFINER` RPC самоудаления был отвергнут после того, как Security
Advisor отметил открытую поверхность, и удалён миграцией. Удаление аккаунта работает
через аутентифицированную edge-функцию `supabase/functions/delete-account`, развёрнутую
на новом сервере.

Секреты сгенерированы заново при переезде; ротация `service_role`, висевшая открытым
вопросом, произошла сама. `app/.env` добавлен в `.gitignore` — до 27.08 он там не был, и
файл с ключами лежал в рабочем дереве.

## Что осталось до полноценного прода

Подробно и с обоснованием — в [`PRODUCTION_READINESS.md`](PRODUCTION_READINESS.md).
Кратко, блокирующее:

1. резервных копий нет ни одной;
2. пользователи и их записи не перенесены из облачного проекта;
3. наблюдаемости нет — первым мониторингом является владелец с телефоном;
4. что `reboot` поднимает стек сам — предположение, а не факт;
5. выгрузка и удаление аккаунта не проверены живым сценарием на новом развёртывании.

Продуктовое, отложенное осознанно: полные контролы редактирования Связок, интерфейс
фонового пластыря, вывод `awareness_content`.

Из релизного гейта v3.0 закрылись оба пункта, которые раньше были невыполнимы:
`supabase start` и `db reset` больше не нужны как формальность — база теперь физически
поднимается из миграций, и это проверено на чистом стеке.

`Together` остаётся v3.1. Admin/Product Intelligence остаётся v3.2.

## Release discipline

Развёртывание alpha для реального тестирования **не** означает, что релизный гейт
закрыт. `v3.0 RELEASED` резервируется до прохождения
`releases/v3.0-platform/REQUIREMENTS.md` и `VALIDATION.md`.
