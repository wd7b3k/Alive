# T-20260905-11 · Выложить на прод всё, что сделано и лежит невыложенным — постановка

    Дата: 05.09.2026
    Тема: release
    Ветка: task/T-20260905-11
    Chat: —

Постановка целиком лежит в `docs/tasks/T-20260905-11-release-publish-backlog.md` и была
закоммичена этой же сессией в составе корзины Б (PR #83): до того она существовала только
в рабочей копии владельца.

Здесь — то, что постановка требовала от исполнителя, коротко, чтобы запись читалась без
переходов:

- **Р1.** Развести номер `T-20260905-01`, означавший две разные работы, и перенумеровать
  ADR-0017 в ветке `feat/pilot-feedback`; проверить, не занят ли какой-то свободный номер
  дважды в других ветках корзины В.
- **Р2.** Слить корзину Б (незакоммиченные постановки, запись сессии 20.08), затем
  корзину А (`task/T-20260905-01`, `chore/nothing-unverified-rule`, `chore/ops-fixes`,
  `feat/server-apply-migrations`) — от инфраструктуры к продукту, от старого к новому. На
  каждой ветке до PR зелёные `tsc`, `eslint`, `prettier`, `vitest`, `build`,
  `scan-bundle-for-secrets`, `board.mjs check`. Ветка, не сливающаяся механически, уезжает
  в корзину В — героического разрешения чужих конфликтов в задаче нет.
- **Р3.** Поднять `version`, дописать одну запись в `app/src/redesign/releases.ts` —
  заголовок и абзац, формат от 27.08 без поля `changes`.
- **Р4.** Дамп базы, выкладка с `main`, сверка отпечатка, шесть пунктов живой проверки,
  мониторинг. Порядок отката написать **до** выкладки.

Корзина В — не сливать без решения владельца: `task/knowledge-base`, `feat/pilot-feedback`,
три версии `agent/monitoring-truth`, `task/counter-health`, `agent/source-funnel-rebased`,
`merge/pilot-with-vps` и августовские архивные ветки. Стоп-лист: миграции каталогов фактов
и мифов, `CONSENT_VERSION`, медицинские формулировки, монетизация, RLS, ключи.

Отдельным риском постановка называла миграции задним числом в трёх ветках корзины В:
проверить по коду применятеля, что он их не пропустит и не применит вне порядка.

## Решения владельца, полученные по ходу

1. **Номер версии — 3.3.0.** Из трёх предложенных (3.3.0, 3.2.1, 4.0.0) выбран
   добавляющий минор: ломающих изменений для участника нет, а 3.2.1 не появился бы на
   публичной странице вовсе — она показывает только мажор и минор.
2. **Выкладку показать планом до исполнения.** План с порядком отката был написан и
   утверждён отдельно; после утверждения выкладка выполнена из сессии.
