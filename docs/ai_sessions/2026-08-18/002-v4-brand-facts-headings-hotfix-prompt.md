# Prompt

Owner reported that the active frontend still had a wrong logo, asked where the «Факты» and «Мифы» sections were, reiterated the prior requirement to remove periods from headings, and required a concrete testable result rather than local artifacts.

Required outcome:

- work from the actual active v4 branch, not stale `main`;
- restore the exact approved logo;
- restore Facts/Myths user surfaces;
- remove periods from headings;
- publish a new release preview on GitHub/Cloudflare;
- keep the repository as the only source of truth.
