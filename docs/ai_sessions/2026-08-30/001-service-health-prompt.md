# Запрос владельца — 30.08.2026

Сессия шла в два захода. Первый — раскатка и починка мониторинга, второй — раздел.

## Заход 1

> Работаем в воркtree Alive.worktrees/observability.
>
> 1. Отправь ветку fix/checks-auth-and-bridge (7a8ea7a) в origin. Локальный origin/main
>    протух — сначала git fetch.
> 2. Дальше всё на сервере, ходи туда сам: ssh alive, каталог /srv/alive/repo.
> 3. Раскатка там падает на гейте миграций: в репозитории 58, в базе 57. Причина:
>    последняя миграция применена напрямую через psql, но не записана в
>    supabase_migrations.schema_migrations. НЕ вставляй вслепую. Сначала посмотри
>    структуру таблицы и последние записи, сверь список файлов миграций с содержимым
>    таблицы, определи какой версии не хватает — и только потом вставляй именно её.
> 4. В app/.env переменные VITE_YANDEX_METRIKA_ID и VITE_GA_MEASUREMENT_ID существуют,
>    но пустые — поэтому grep -q их «находил» и ничего не дописывал. Задай значения:
>    111983810 и G-YB35G45MFW.
> 5. Запусти /srv/alive/deploy.sh.
> 6. Проверь счётчики по НОМЕРУ, а не по строке mc.yandex.ru — она в коде всегда.
>    В бандле должны найтись 111983810 и G-YB35G45MFW.
> 7. Прогони infra/monitoring/run-check.sh fast, slow, daily и покажи ops.check_results.
>    auth_health и yandex_bridge должны стать ok, build_fingerprint — показать новый
>    коммит.
>
> Ничего не проси у меня выполнять — делай сам и покажи результат.

## Заход 2

> Переходи к реализации раздела технического мониторинга основных метрик здоровья
> сервиса на уровне сервисов, всего бэка и фронта. Предусмотри, что еще появятся
> альтернативные фронты, типа ТГ бота, мобильного приложения и пр.
