# Честный build_fingerprint

Запрос владельца, 30.08.2026. Сессия B из пяти.

Карточка: `proverka-otpechatka-sborki-sravnivaet`.

Сейчас `infra/monitoring/checks/http.sh` сравнивает `version.json` сам с собой.
Проверка должна сравнивать коммит живой сборки с `origin/main` (логика уже есть в
`scripts/check-deploy-drift.mjs`) и сверять имя JS-чанка из отданного `index.html` с
тем, что лежит в каталоге `current`.

Приёмка: подменить сборку без выкладки — проверка обязана упасть; показать вывод, где
она падает, и вывод, где проходит.

Правила сессии: одна ветка от актуального `origin/main`, один критерий приёмки;
карточка закрывается трейлером `Board: done` в том же коммите, найденное попутно —
`Board: new` там же; прод правится только выкладкой из репозитория, не руками по ssh;
проверка фактами — вывод команды, а не «должно работать».
