# Шеринг: поле url, метка источника и карточка превью

Запрос владельца, 30.08.2026. Сессия D из пяти.

Карточки: `podelitsya-ssylkoy-bez-polya`, `prevyu-ssylki-pokazyvaet-tolko`.

В `app/src/redesign/share.tsx` передавать адрес отдельным полем `url`, а не внутри
`text`. Добавить к ссылке метку источника, читаемую собственными счётчиками
(`app/src/services/counters.ts`), без внешних сервисов. Заменить `og:image`: сейчас это
логотип на тёмном фоне, нужна карточка 1200×630, на которой видно, что это за сервис.
Текст на картинке не должен содержать медицинских обещаний — ограничения в `AGENTS.md`.

Приёмка: превью проверено в Telegram после сброса кэша через `@WebpageBot` и показано в
ответе; тест на `shareText` обновлён.

Правила сессии: одна ветка от актуального `origin/main`, один критерий приёмки;
карточки закрываются трейлером `Board: done` в том же коммите; никаких внешних сервисов
в рантайме прода; прод правится только выкладкой из репозитория; проверка фактами.
