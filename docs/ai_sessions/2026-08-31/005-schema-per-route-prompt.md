# Доделать выкладку и разметку

Запрос владельца, 31.08.2026, файлом `HANDOFF.md` в корне рабочей копии.
PR #56 влит владельцем, `origin/main` = `364fd15`.

## 1. Выложить и поставить Caddyfile

Прод отстаёт, и без нового `Caddyfile` страница 404 останется пустой: `handle_errors` со
`status 404` живёт в конфигурации, а не в сборке. Порядок — `infra/caddy/README.md`, ключ
брать из действующего конфига до замены, `caddy validate` перед `reload`, откат оттуда же.

Приёмка, показать вывод каждой: `version.json` равен `origin/main` и
`check-deploy-drift.mjs` нулём; `curl -sI https://habitoff.ru/net-takogo-adresa` → `404`,
тело непустое, в нём шесть ссылок и `noindex, follow`; тела шести публичных адресов
различаются, у каждого свой `<h1>`; вход через Яндекс отвечает 303 на `oauth.yandex.ru`.

## 2. Разметка по месту

Замер снаружи 31.08: одна и та же разметка на всех шести адресах —
`WebSite + WebApplication + Offer + FAQPage + Question×5`. `FAQPage` описывает главную, а
стоит везде. Пререндер подставляет своё тело, а разметку копирует целиком.

- `FAQPage` — только на `/`.
- `/knowledge` — своя разметка по опубликованному каталогу: `ItemList` из
  `Question`/`Answer`, у каждого `citation` на источник. Утверждение без источника в
  разметку не попадает.
- `/links`, `/meanings`, `/experiment`, `/releases` — своя по смыслу раздела.
- `BreadcrumbList` на внутренних адресах.
- `Organization`/`sameAs` не заводить: адресов сообществ ещё нет.

Приёмка: `FAQPage` встречается ровно на одном адресе; разметка `/knowledge` проходит
валидатор Schema.org; в ней нет утверждения без `citation`.

## 3. Прибраться на origin

`work/seo-links-prerender` равна `main` — удалить. Проверить `agent/monitoring-truth`: по
отчёту прошлой сессии на неё могли лечь три чужих коммита. Если легли — не переписывать
чужую ветку молча, а сказать владельцу, что именно там лежит.

## Правило, которого не хватало

**Один воркtree — одна сессия.** 31.08 две сессии работали в
`Alive.worktrees/observability` одновременно: правки в `redesign.css` молча исчезли при
переключении ветки, три коммита легли на чужую ветку, а `push` отрапортовал успех,
запушив пустую. Записать в `CLAUDE.md`; если он ещё untracked — положить правило в
`docs/` и сказать об этом в отчёте.
