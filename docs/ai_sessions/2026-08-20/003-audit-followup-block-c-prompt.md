# Prompt — блок C: реальные privacy/security проверки без живых credentials

Дата: 2026-08-20

## Контекст

Продолжение после блоков A и B (сессия 002). Владелец:

> давай делай дальше

— в контексте предыдущего явного ограничения: блок C (RLS isolation, secret scan,
UGC-consent, export/delete) требует живых Supabase/Google OAuth credentials, которых
нет ни в облачной песочнице (сеть есть, секретов владельца нет), ни через мост к
устройству владельца (файлы есть, сети нет).

## Требования

Сделать максимум из блока C, что реально достижимо **без** живых production-секретов:

- RLS isolation (`RISK-V3-001`): если нельзя против живого Supabase — проверить хотя
  бы саму SQL-логику политик из реальных migrations репозитория каким-то воспроизводимым
  способом.
- Secret scan (`RISK-V3-002`): статическая проверка production-бандла на секреты —
  это не требует живых credentials вообще, только собранный `dist/`.
- UGC-consent (`RISK-V3-008`): структурная проверка схемы/кода на отсутствие
  автопубликации.
- Export/delete (gate #9, "Still required" в CURRENT_STATE.md): реализовать то, что
  можно реализовать как код без деплоя — data-layer функции и Edge Function.

Не выдавать локальные/структурные проверки за эквивалент живого теста — явно
разграничивать в VALIDATION.md, что подтверждено, а что нет. Не объявлять релиз.
